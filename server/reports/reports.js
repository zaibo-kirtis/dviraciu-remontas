let express = require( 'express' );
let db = require( './../database' );

let queries = {
};

let reportsController = express.Router();

//reportsController.post( '/orders', getSomeReport );

module.exports = reportsController;